if [ "$2" = "d" ]; then
   rm out.txt
fi
 
if [ "$#" != 0 ]; then
   

	for ((  i = 0 ;  i < "$1";  i++  ))
	do
	python capture.py -r BaselineAgents -b NinjaAgents -q >> out.txt
	done
	
	python parse.py
else
    echo "Mistake in arguments."
fi